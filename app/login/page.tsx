"use client";
import Link from "next/link";import {createBrowserClient} from "@supabase/ssr";
export default function Login(){async function login(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){alert('Seekly needs the Supabase environment variables added in Vercel first.');return}const s=createBrowserClient(url,key);await s.auth.signInWithOAuth({provider:'google',options:{redirectTo:`${location.origin}/auth/callback`,scopes:'openid email profile https://www.googleapis.com/auth/gmail.readonly https://www.googleapis.com/auth/drive.readonly',queryParams:{access_type:'offline',prompt:'consent'}}})}return <main className="authwrap"><div className="authcard"><Link className="brand" href="/">Seekly<span>.</span></Link><h1>Search your business</h1><p>Connect your Google account to securely search Gmail and Drive from one place.</p><button className="google" onClick={login}>G&nbsp;&nbsp; Continue with Google</button><p style={{fontSize:11}}>Read-only access. Seekly cannot send email or edit your files.</p></div></main>}